import React from 'react';
import Layout from '../../components/Layout';
import Nav from '../../components/Nav';

const Plan = ()=>(
    <Layout>
        <Nav/>
        <h1>Plan</h1>
    </Layout>
);

export default Plan;